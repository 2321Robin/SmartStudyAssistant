spring:
  datasource:
    url: ${SMARTSTUDY_USER_DB_URL:jdbc:mysql://localhost:3306/smartstudy_user?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${SMARTSTUDY_USER_DB_USERNAME:root}
    password: ${SMARTSTUDY_USER_DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

server:
  port: 8081
